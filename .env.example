# Copy to .env and fill in real secrets locally.
# Do not commit real API keys, private corpus files, generated indexes, or traces.

# AI provider credentials
# Required for answer generation, OpenAI embeddings, vector indexing, and OCR via ChatOpenAI.
OPENAI_API_KEY=

# Alternative key checked by the runtime before enabling semantic/OCR paths.
# Leave blank unless your local LangChain/OpenAI setup is wired for Azure OpenAI.
AZURE_OPENAI_API_KEY=

# Optional OCR-specific configuration gate checked by ingestion.
# The current OCR client still authenticates through the LangChain/OpenAI environment.
IMPERIAL_RAG_OCR_API_KEY=

# Optional reranker provider key for future/experimental Cohere reranking paths.
COHERE_API_KEY=

# Local workspace
IMPERIAL_RAG_WORKSPACE_ROOT=/Users/danil/Public/imperial

# Qdrant vector store
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=imperial_chunks

# Set to 1 only when intentionally running live Qdrant tests.
IMPERIAL_RAG_LIVE_QDRANT=0

# Phoenix tracing and eval storage
PHOENIX_PROJECT_NAME=imperial-rag
PHOENIX_COLLECTOR_ENDPOINT=http://localhost:6006/v1/traces
PHOENIX_CLIENT_ENDPOINT=http://localhost:6006
PHOENIX_TRACING_ENABLED=false
IMPERIAL_RAG_TRACING_ENABLED=false

# Retrieval and chunking tuning
IMPERIAL_RAG_CHUNK_SIZE=400
IMPERIAL_RAG_CHUNK_OVERLAP=50
IMPERIAL_RAG_VECTOR_FETCH_K=80
IMPERIAL_RAG_VECTOR_K=32
IMPERIAL_RAG_KEYWORD_LIMIT=40
IMPERIAL_RAG_RERANK_INPUT_LIMIT=60
IMPERIAL_RAG_RERANK_TOP_N=12
IMPERIAL_RAG_NEIGHBOR_WINDOW=1
IMPERIAL_RAG_FINAL_EVIDENCE_MIN=18
IMPERIAL_RAG_FINAL_EVIDENCE_MAX=24
IMPERIAL_RAG_MMR_LAMBDA_MULT=0.4
IMPERIAL_RAG_PRIMARY_RERANKER=cohere:rerank-v3.5
IMPERIAL_RAG_FALLBACK_RERANKER=cohere:rerank-multilingual-v3.0
